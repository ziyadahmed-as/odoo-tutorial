<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Tree View ────────────────────────────────────────── -->
    <record id="view_library_loan_tree" model="ir.ui.view">
        <field name="name">library.loan.tree</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <tree decoration-muted="state == 'returned'" decoration-danger="state == 'overdue'" decoration-info="state == 'active'">
                <field name="book_id"/>
                <field name="partner_id"/>
                <field name="loan_date"/>
                <field name="return_date"/>
                <field name="state" widget="badge" decoration-muted="state == 'returned'" decoration-danger="state == 'overdue'" decoration-info="state == 'active'"/>
            </tree>
        </field>
    </record>

    <!-- ── Form View ────────────────────────────────────────── -->
    <record id="view_library_loan_form" model="ir.ui.view">
        <field name="name">library.loan.form</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirm Loan" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_return_book" string="Return Book" type="object" class="btn-primary" invisible="state not in ('active', 'overdue')"/>
                    <button name="action_reset_draft" string="Reset to Draft" type="object" groups="library_management.group_library_manager" invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,returned"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="book_id" readonly="state != 'draft'"/>
                            <field name="partner_id" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="loan_date" readonly="state != 'draft'"/>
                            <field name="return_date" readonly="state != 'draft'"/>
                            <field name="actual_return_date" invisible="state != 'returned'"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ── Action ───────────────────────────────────────────── -->
    <record id="action_library_loan" model="ir.actions.act_window">
        <field name="name">Loans</field>
        <field name="res_model">library.loan</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new loan record!
            </p>
        </p>
    </record>

</odoo>
