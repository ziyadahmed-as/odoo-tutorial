<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Tree View ────────────────────────────────────────── -->
    <record id="view_library_book_tree" model="ir.ui.view">
        <field name="name">library.book.tree</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <tree decoration-info="status == 'available'" decoration-warning="status == 'borrowed'" decoration-danger="status == 'lost'">
                <field name="title"/>
                <field name="author"/>
                <field name="isbn"/>
                <field name="publication_date"/>
                <field name="genre"/>
                <field name="status" widget="badge" decoration-info="status == 'available'" decoration-warning="status == 'borrowed'" decoration-danger="status == 'lost'"/>
            </tree>
        </field>
    </record>

    <!-- ── Form View ────────────────────────────────────────── -->
    <record id="view_library_book_form" model="ir.ui.view">
        <field name="name">library.book.form</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_borrow" string="Borrow" type="object" class="btn-primary" invisible="status != 'available'"/>
                    <button name="action_return" string="Return" type="object" class="btn-primary" invisible="status != 'borrowed'"/>
                    <button name="action_mark_lost" string="Mark Lost" type="object" invisible="status == 'lost'"/>
                    <field name="status" widget="statusbar" statusbar_visible="available,borrowed,lost"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_loans" type="object" class="oe_stat_button" icon="fa-retweet">
                            <field name="loan_count" widget="statinfo" string="Loans"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="title" placeholder="Book Title..."/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="author"/>
                            <field name="isbn"/>
                        </group>
                        <group>
                            <field name="publication_date"/>
                            <field name="genre"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Loan Info" name="loan_info">
                            <group>
                                <field name="active_loan_id" invisible="status != 'borrowed'"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ── Action ───────────────────────────────────────────── -->
    <record id="action_library_book" model="ir.actions.act_window">
        <field name="name">Books</field>
        <field name="res_model">library.book</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Register your first library book!
            </p>
        </p>
    </record>

</odoo>
